<!DOCTYPE html>
<html>

<head>
    <title>Netshow</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <script src="https://kit.fontawesome.com/4c32ab11ef.js"></script>
    <link href="https://fonts.googleapis.com/css?family=Open+Sans|Saira+Stencil+One&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>
    <div class="container">
        <% if notice %>
            <div class="alert alert-info" role="alert">
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                  </button>
              <p id="notice"><%= notice %></p>
            </div>
        <% end %>

        <header class="blog-header py-3">
            <div class="row flex-nowrap justify-content-between align-items-center">
                <div class="col-4">
                    <a class="logo" href="<%= root_path %>">Netshow.me</a>
                </div>
                <div class="col-4 d-flex justify-content-end align-items-center">
                    <% if user_signed_in? %>
                    <h3 class="m-left"><i class="fas fa-user"></i> Olá,
                        <%= current_user.username %>!</h3>
                    <%= link_to new_movie_path, class: "btn btn-sm btn-primary m-left" do %>
                    New Movie <i class="fas fa-plus"></i>
                    <% end %>
                    <%= link_to destroy_user_session_path, method: :delete, class: "btn btn-sm btn-outline-primary" do %>
                    Log Out <i class="fas fa-sign-out-alt"></i>
                    <% end %>
                    <% else %>
                    <a href="/users/sign_in" class="btn btn-sm btn-outline-primary m-left">Login <i class="fas fa-sign-in-alt"></i></a>
                    <a href="/users/sign_up" class="btn btn-sm btn-primary">Sign up <i class="fas fa-sign-in-alt"></i></a>
                    <% end %>
                </div>
            </div>
        </header>
        <div class="nav-scroller py-1 mb-2">
            <nav class="nav d-flex justify-content-between">
                <% Category.all.order(:name).each do |category| %>
                <a class="p-2" href="/categories/<%= category.id %>">
                    <%= category.name %>
                </a>
                <% end %>
            </nav>
        </div>
    </div>
    <div class="content">
        <%= yield %>
    </div>
</body>
<script src="https://code.jquery.com/jquery-3.3.1.slim.min.js" integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo" crossorigin="anonymous"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js" integrity="sha384-UO2eT0CpHqdSJQ6hJty5KVphtPhzWj9WO1clHTMGa3JDZwrnQq4sF86dIHNDz0W1" crossorigin="anonymous"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js" integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM" crossorigin="anonymous"></script>

</html>